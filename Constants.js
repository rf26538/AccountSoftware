const path = require("path");

module.exports = {
    TOKEN_FILE_PATH: path.join(__dirname, '/Token.json'),
    CREDENTIAL_FILE_PATH: path.join(__dirname, '/Credential.json'),
    DOWNLOAD_FOLDER_PATH: path.join(__dirname, '/Downloads'),
}